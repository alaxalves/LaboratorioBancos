<%= form_with(model: papel, local: true) do |form| %>
  <% if papel.errors.any? %>
  <div class="alert alert-danger alert-dismissible" role="alert" id="error_explanation">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
    <h2><%= pluralize(papel.errors.count, "error") %> prohibited this papel from being saved:</h2>
    <ul>
      <% papel.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <div class="form-box">
    <div class="form-group">
      <%= form.label :descricao %>
      <%= form.text_area :descricao, id: :papel_descricao, class:'form-control' %>
    </div>
    <div class="actions">
      <%= form.submit class:'btn btn-success' %>
    </div>
  </div>
<% end %>
